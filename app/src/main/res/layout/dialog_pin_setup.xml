<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🛡️ Set NoteNova PIN Lock"
        android:textColor="?attr/colorPrimary"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="Choose a secure 4-Digit password to encrypt your locked folders and secure sheets."
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="13sp" />

    <EditText
        android:id="@+id/etSetupPin"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:hint="- - - -"
        android:inputType="number"
        android:textColor="?attr/colorOnSurface"
        android:textSize="24sp"
        android:maxLength="4" />

    <Button
        android:id="@+id/btnSetupPinSubmit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Save Passcode Lock" />

</LinearLayout>
