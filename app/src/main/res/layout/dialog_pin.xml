<?xml version="1.0" encoding="utf-8"?>
<LinearLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="wrap_content"
    android:orientation="vertical"
    android:padding="24dp"
    android:gravity="center">

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:text="🔒 NoteNova Safe Box"
        android:textColor="?attr/colorPrimary"
        android:textSize="20sp"
        android:textStyle="bold" />

    <TextView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginTop="8dp"
        android:gravity="center"
        android:text="This note is encrypted. Enter your 4-Digit Security Passcode PIN to unlock."
        android:textColor="?attr/colorOnSurfaceVariant"
        android:textSize="13sp" />

    <EditText
        android:id="@+id/etDialogPin"
        android:layout_width="160dp"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:gravity="center"
        android:hint="* * * *"
        android:inputType="numberPassword"
        android:textColor="?attr/colorOnSurface"
        android:textSize="24sp"
        android:maxLength="4" />

    <Button
        android:id="@+id/btnDialogPinSubmit"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginTop="16dp"
        android:text="Authenticate &amp; Open" />

</LinearLayout>
